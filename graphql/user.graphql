type User {
    id: ID!
    name: String!
    age: Int!
    email: String!
    is_adult: Boolean! @method(name: isAdult)
    created_at: DateTime!
    updated_at: DateTime!
    posts: [Post!] @hasMany
    postsIn(year: Int!): [Post!] @method # TODO
    notes: [Note!] @hasMany
}
